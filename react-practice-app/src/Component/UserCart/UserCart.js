import React from 'react';


const UserCart = (props) => {
    console.log(props.user);
    return (
        <div>
            <h1>Addeed User list</h1>
        </div>
    );
};

export default UserCart;