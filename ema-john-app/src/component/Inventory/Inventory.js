import React from 'react';

const Inventory = () => {
    document.title = 'Inventory'
    return (
        <div>
            <h2>This is inventory</h2>
        </div>
    );
};

export default Inventory;